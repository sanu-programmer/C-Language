#include<stdio.h>

int main(){
    int x;
    float y;
    double a;
    char b;
    printf("The size of integer is: %d" ,sizeof(x));
    printf("\nThe size of float is:%d" ,sizeof(y));
    printf("\nThe size of double is:%d" ,sizeof(a));
    printf("\nThe size of character is:%d" ,sizeof(b));
}